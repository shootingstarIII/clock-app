const Clock = () => {
  return (
    <div>
      {/* <div className="font-inter text-h3 flex items-center tracking-[4.8px]">
        <span className="inline pr-2">
          {MyIcon(isDayTime() ? IconSun : iconMoon)}
        </span>
        <p>GOOD MORNING, ITS CURRENTLY</p>
      </div>
      <div>
        <p className="font-inter text-h1 bold uppercase">{currentTime}</p>
        <p>PM</p>
      </div>
      {location && (
        <div className="font-inter text-h3 text-bold uppercase tracking-[4.8px]">
          IN {location.city},{location.country}
        </div>
      )} */}
    </div>
  );
};

export default Clock;
